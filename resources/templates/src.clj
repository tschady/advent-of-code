(ns aoc.{{year}}.d{{day}}
    (:require
     [aoc.file-util :as f]
     [aoc.string-util :as s]))

(def input (f/read-file "{{year}}/d{{day}}.txt"))

(defn part-1 [input] true)

(defn part-2 [input] true)
